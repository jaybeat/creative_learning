# 第1章 绪论

> 说明：本文件汇总了讨论中写好的第1章各节文字，并按全书统一的节号编排。标有【原稿】的位置是你已写好、本次未改动的部分，保留原文接入即可。

## 1.1 数据结构要解决什么问题

【原稿："如何设计数据的组织方式，让管理和操作数据的效率更高"这一定义及引入】

## 1.2 数据的逻辑关系

### 1.2.1 线性关系：单行编辑器

【原稿：线性关系 / 单行编辑器一段】

数据的逻辑关系有四种，除了单行编辑器这种序列（也叫线性关系），还有树形关系、图关系和集合关系。

### 1.2.2 树形关系：文件夹

比如我们电脑的文件夹，文件夹下有文件和二级文件夹，二级文件夹下面可以继续包含文件和文件夹，这些文件数据之间存在着多层级的关联关系，这叫树形关系或者层级关系。而我们日常需要对电脑新增文件、删除文件、读取文件、修改文件等各种操作，因此可以把文件管理的问题描述为树形模型+操作集。

### 1.2.3 图关系：地图

类似的，还有我们日常使用的地图软件。地图上的路口和地点通过一条条路段相互连接，一个路口可以通向多个方向，两个地点之间往往有多条走法，这种"多对多、可成环"的关联关系叫图关系。它和树形关系的本质区别在于：树中任意结点只有一个上级，从根到任意结点的路径唯一，不能成环；而图中任意两个元素之间都可以有关联，而且可以绕一圈回到原点。地图软件需要回答的问题，比如"从家到公司最快怎么走""附近三公里内有哪些加油站""某条路封闭后如何改道"，本质上都是在图上查找路径、遍历邻居、增删边。因此导航问题可以描述为图模型+操作集。

### 1.2.4 集合关系：拦截名单

集合关系我们在初高中数学里已经学过：一个元素要么属于某个集合，要么不属于；集合里的元素没有先后之分，{1, 2, 3} 和 {3, 1, 2} 是同一个集合；集合里也不会出现重复的元素。也就是说，集合里的元素之间除了"同属一个集合"外，没有任何别的联系——这是最松散的一种逻辑关系。不同的是，数学里的集合往往是给定不变的，而程序中的集合会随着运行不断有元素加入或移出。比如手机的骚扰电话拦截功能。系统维护着一份拦截名单，来电时只需要判断这个号码是否在名单中，至于号码是何时加入的、排在第几位，都无关紧要。名单中的号码之间除了"同属这份名单"之外没有别的联系，这就是集合关系。我们对名单的操作也很简单：加入、移除、查询是否存在。因此拦截功能可以描述为集合模型+操作集。

把四种关系放在一起看，它们是递进的：集合是"只归属、无关联"，线性是"一对一"，树是"一对多"，图是"多对多"。

> **到这里你知道了**：数据之间的逻辑关系只有四种：集合（只归属）、线性（一对一）、树（一对多）、图（多对多）。
> **每个例子的共同写法**：某种关系的模型 + 一组操作。这个写法下一节会给它一个名字。

## 1.3 抽象数据类型 = 逻辑关系 + 操作集

### 1.3.1 抽象数据类型的定义

【原稿：抽象数据类型 = 逻辑关系 + 操作集 的引入】

### 1.3.2 同一逻辑关系，不同的操作集

需要注意，逻辑关系和抽象数据类型并不是一一对应的。同一种逻辑关系，放到不同的应用场景里，需要的操作往往不一样，于是就会定义出不同的抽象数据类型。

以线性关系为例：单行编辑器需要在任意位置插入和删除字符，这是线性表；浏览器的"后退"功能只需要记住最近访问的页面，并且总是最后打开的最先退回，只在一端进出，这是栈；打印机处理任务时先提交的先打印，一端进、另一端出，这是队列。三者的元素之间都是一对一的前后关系，区别完全在操作集上。

集合关系也一样：骚扰电话名单只需要增、删和判断是否存在，是集合；通讯录要按姓名查号码，元素是"姓名→号码"的对子，核心操作是"按键取值"，是字典；社交软件判断两个人是否在同一个朋友圈、把两个圈子合并，核心操作是"合并两个集合"和"判断两个元素是否同属一个集合"，是并查集。这三者元素之间都只有"归属"关系，但需求不同，就长成了三个完全不同的抽象数据类型。

树形关系同样如此：文件夹管理关注遍历和增删结点，而按学号快速查找学生所用的二叉搜索树，关注的是按键查找。操作不同，抽象数据类型就不同。

反过来，同一个抽象数据类型也可以有不同的存储实现方式，这是后面要讨论的问题。

### 1.3.3 为什么不把所有操作放进一个操作集

回头看"抽象数据类型 = 逻辑关系 + 操作集"这个公式。逻辑关系只有四种基本形态，它回答的是"数据之间怎么关联"；操作集回答的是"要对数据做什么"。现实中的问题最终都要落实到具体的数据操作上来解决，比如地图查"从家到公司的路线"，就是在图上查找路径的操作。所以，真正把一个个抽象数据类型区分开的，是操作集。

有人可能会问：既然逻辑关系只有四种，为什么不在每种逻辑关系上，把可能用到的操作全部放进一个操作集，这样一种逻辑关系只定义一个抽象数据类型，岂不是更省事？

从逻辑上看，这样定义并没有错。但这么做有两个问题。

第一，从使用的角度看，操作集越精简，含义越清楚。栈只允许在一端进出，不是因为做不到别的，而是这个约束本身就在传递信息：看到"栈"，使用者立刻知道它是用来处理"后进先出"问题的，也不会在中间随意插入元素而破坏这个性质。把所有操作都开放出来，这层含义就没有了。

第二，从实现的角度看，回到"数据结构"这门课要解决的问题——"如何设计数据的组织方式，让管理和操作数据的效率更高"，关键词是"效率"。抽象数据类型最终要映射到计算机的存储上去实现，而几乎不存在一种存储方式能让所有操作都高效：让一部分操作快，往往要以另一部分操作慢为代价。操作集小而明确，我们才能针对它挑选最合适的存储方式。

所以，下一个问题自然就是：抽象数据类型是怎么映射到计算机存储上的？这是1.4节的内容。

> **到这里你知道了**：抽象数据类型 = 逻辑关系 + 操作集；逻辑关系只有四种，真正区分不同抽象数据类型的是操作集。
> **下一步**：抽象数据类型要落到内存里，看内存能提供什么。

## 1.4 存储结构

### 1.4.1 内存是什么样的

要把数据放进计算机，先要知道计算机的内存长什么样。内存可以看成一长排编了号的格子，每个格子存放一个字节，格子的编号就是地址。程序要读写数据，就是按地址去找对应的格子。

这排格子有一个特点：它只有一种天然的关系，就是"前后相邻"。第100号格子的后面是第101号，仅此而已。也就是说，内存本身是一维的、线性的。

### 1.4.2 关系存在哪里：用位置表示（顺序存储）

这就带来一个问题。数据本身放进格子没有困难，一个整数占几个格子，一个字符占一个格子。但逻辑关系里的"关系"放在哪里？比如线性关系中"A的下一个是B"，树形关系中"A的孩子是B和C"，图关系中"A和B之间有一条路"——这些信息不是数据本身，它们该存在什么地方？

办法只有两种。

我们用一个具体的序列来看这两种办法。假设要存的是四个数：**12, 7, 30, 5**，它们的逻辑关系是线性的，7在12后面，30在7后面，5在30后面。为了讲清楚原理，我们先假设一个格子刚好放得下一个数（真实情况下一个整数通常占4个字节，即4个格子，这里暂时忽略）。

第一种办法是用位置表示关系。做法是，先向内存申请一段连续的格子，比如从100号开始的一段，然后把元素按逻辑顺序依次放进去：

| 地址 | 100 | 101 | 102 | 103 |
|---|---|---|---|---|
| 内容 | 12 | 7 | 30 | 5 |

注意，我们并没有在任何地方写下"7在12后面"这条关系。但这条关系已经在了：因为我们事先约定"相邻格子放相邻元素"，7在101号、12在100号，两个格子相邻，关系就隐含在位置里。存关系不需要占任何额外空间。

要取第3个元素怎么办？只需要知道起始地址是100，那么第3个元素的地址就是 100 + (3 − 1) = 102，直接去102号格子取出30。不管序列有多长，取第几个，都是这样一步算出来，不需要经过前面的元素。

但要在7和30之间插入一个数，比如20，就麻烦了。20必须放在102号格子，才能维持"相邻即相邻"的约定，可102号已经被30占着。只能先把5从103挪到104，再把30从102挪到103，腾出102号再放20：

| 地址 | 100 | 101 | 102 | 103 | 104 |
|---|---|---|---|---|---|
| 内容 | 12 | 7 | 20 | 30 | 5 |

插入位置越靠前，序列越长，要挪动的元素就越多。删除也一样，删掉中间一个，后面的都要往前挪一格补上空缺。

### 1.4.3 关系存在哪里：用地址表示（链式存储）

第二种办法是用地址表示关系。做法是，每个元素占两个格子：一个放数据，一个放"下一个元素在哪"的地址。元素不必挨在一起，可以散落在内存的任何地方。比如四个元素分别放在200、350、120、410号：

| 地址 | 内容 | 说明 |
|---|---|---|
| 200 | 12 | 数据 |
| 201 | 350 | 下一个元素在350号 |
| 350 | 7 | 数据 |
| 351 | 120 | 下一个元素在120号 |
| 120 | 30 | 数据 |
| 121 | 410 | 下一个元素在410号 |
| 410 | 5 | 数据 |
| 411 | 空 | 没有下一个了 |

这一次，"7在12后面"这条关系被明确写在了201号格子里：12的旁边记着350，去350号就能找到7。只要记住第一个元素在200号，就能顺着地址一路走完整个序列：200→350→120→410。四个元素在内存里的物理位置是乱的，但逻辑顺序完全靠这些地址串了起来。

要取第3个元素，就没有办法一步算出地址了。因为元素放在哪里没有规律，只能从200号出发，看201号知道下一个在350，看351号知道再下一个在120，走到第3站取出30。序列越长、要取的位置越靠后，走的步数就越多。

但要在7和30之间插入20，就很轻松了。随便找两个空格子，比如500、501，把20放进500号，然后改两个地址：501号写上120（20的下一个是30），351号改成500（7的下一个是20）：

| 地址 | 内容 |
|---|---|
| 350 | 7 |
| 351 | 120 → 500 |
| 500 | 20 |
| 501 | 120 |
| 120 | 30 |

其他元素一个都没动。不管序列多长，插入一个元素永远只是改两个地址。删除同理，把前一个元素的"下一个"地址跳过被删的那个就行。

代价也很明显：每个元素多占了一个格子来存地址，序列有多长，就多用多少空间。

### 1.4.4 两种办法的对比

| | 顺序存储 | 链式存储 |
|---|---|---|
| 关系存在哪 | 隐含在位置里 | 明确写在地址格子里 |
| 关系占不占空间 | 不占 | 每个元素多占一格 |
| 取第i个元素 | 一步算出地址 | 从头走i步 |
| 中间插入、删除 | 挪动后面所有元素 | 改两个地址 |

可以看到，两种办法各有一头快、一头慢，没有哪一种全面占优。其他存储方式，比如索引存储、散列存储，本质上都是这两种手段的组合或变形，后面用到时再具体介绍。

### 1.4.5 四种逻辑关系怎么存

有了这两种手段，四种逻辑关系都能装进一维的内存。这里先看个大概，具体细节留到后面各章。

线性关系最直接：顺序存储就是数组，元素一个挨一个放；链式存储就是链表，每个元素记下下一个元素的地址。

树形关系：链式存储最自然，每个结点记下它各个孩子的地址；某些形态规整的树（比如完全二叉树）也可以顺序存储，按层给结点编号，用编号的算术关系代替地址。

图关系：可以用一张二维表记录任意两个顶点之间有没有边，这是邻接矩阵，属于顺序存储；也可以给每个顶点挂一条链，记下它连到哪些顶点，这是邻接表，属于链式存储。

集合关系：元素之间没有关系需要记录，关键是"判断某个元素在不在"要快，通常用散列表，把元素的值直接换算成存放位置。

### 1.4.6 效率的代价

现在可以回答上一节留下的问题了。以线性关系为例，比较一下两种存储方式：

- 按位置取第i个元素：顺序存储一步算出地址；链式存储要从头数i步。
- 在中间插入一个元素：顺序存储要挪动后面所有元素；链式存储只改两个地址。

这正是前面说的"让一部分操作快，另一部分操作就会慢"。如果一个应用主要是按位置读取、很少插入删除，顺序存储合适；如果频繁插入删除、很少按位置读取，链式存储合适。哪种更好，取决于操作集里的操作各占多少——这也是为什么我们要把操作集定义得小而明确。

那么，"一步"和"i步""挪动所有元素"这些说法，能不能更精确地衡量？这就是1.5节要讨论的算法和复杂度分析。

> **到这里你知道了**：内存是一维的，关系要么隐含在位置里（顺序存储），要么明确存成地址（链式存储）；两种手段各有一头快、一头慢。
> **下一步**：把"快""慢"说得精确。

## 1.5 算法与复杂度分析

### 1.5.1 什么是算法

前面说，抽象数据类型要映射到存储结构上去实现。所谓"实现"，就是针对操作集里的每一个操作，写出在这种存储结构上具体怎么做的一步步过程。比如"取第i个元素"，在顺序存储上是"用起始地址加上i−1"，在链式存储上是"从第一个元素出发，顺着地址走i−1步"。这样一个明确的、有限步的操作过程，就叫算法。

同一个操作，在不同存储结构上算法不同；即便在同一存储结构上，也常常有不止一种算法。于是就有了一个问题：怎么判断哪个算法更好？

### 1.5.2 用步数代替秒

最直接的想法是把程序跑一遍，看用了几秒。但这个办法有个问题：同一段程序，在快的电脑上跑得快，在慢的电脑上跑得慢；用C语言写和用Python写，时间也不一样。秒衡量的是机器和语言，而不是算法本身。我们想比较的是"方法"的好坏，需要一个跟机器无关的尺子。

换一个思路：不看时间，看算法执行了多少步基本操作。一次加法、一次比较、一次读取格子、一次改写地址，都算一步。步数只取决于算法本身和数据的规模，换台机器、换种语言，步数是一样的。

还是用上一节的序列12, 7, 30, 5来看。取第3个元素，顺序存储是一次加法算出地址、一次读取，2步；链式存储是读200号、读201号、读350号、读351号、读120号，5步。在7和30之间插入20，顺序存储要挪动5和30两个元素、再放入20，3步；链式存储是放入20、改两个地址，3步。

### 1.5.3 只看增长趋势：大O记号

上面这些数字看起来差不多，因为序列只有4个元素。数据结构要处理的是几万、几百万个元素的情况，这时候差距才显出来。

我们用n表示元素个数，再算一遍：

- 顺序存储取第i个元素：不管n多大，都是一次加法一次读取，永远2步。
- 链式存储取第i个元素：要走i−1站，每站读2个格子，最坏i=n时约2n步。
- 顺序存储在位置i插入：要挪动后面n−i个元素，最坏i=1时约n步。
- 链式存储插入：找到位置之后，永远3步。

可以看到，有的步数不随n变化，有的和n成正比。当n是一百万时，"2步"和"两百万步"的差别，远远大于"2步"和"3步"的差别。所以我们比较算法时，真正关心的是：**步数随着n的增长，增长得有多快**。至于具体是n步还是2n步、还是2n+3步，这些常数和零头在n很大时都无关紧要。

为了只保留增长趋势、抹掉常数和零头，我们用一种专门的写法：大O记号。

- 步数不随n变化的，不管是2步还是30步，记作 **O(1)**，读作"常数阶"。
- 步数和n成正比的，不管是n步、2n步还是2n+3步，记作 **O(n)**，读作"线性阶"。

于是上一节的结论可以简洁地写成：

| 操作 | 顺序存储 | 链式存储 |
|---|---|---|
| 取第i个元素 | O(1) | O(n) |
| 中间插入、删除 | O(n) | O(1) |

这张表比"一步算出""从头走i步""挪动后面所有元素"这些描述精确得多，也更容易比较。大O记号描述的是最坏情况下步数随n增长的量级。因为它只看n很大时的趋势，这种分析方法叫**渐进时间复杂度分析**，大O记号写出的量级叫渐进时间复杂度。它就是本书衡量算法效率的标准尺子，后面每一章都会用到。

（注：链式存储的插入、删除是"找到位置之后"的代价，定位本身是O(n)，第2章会精确讨论。）

### 1.5.4 常见的量级

除了O(1)和O(n)，后面还会经常遇到几种量级。这里先列出来，感受一下它们的差距。表中的数字是n取不同值时步数的大致规模：

| 量级 | 读法 | n=10 | n=1000 | n=1000000 |
|---|---|---|---|---|
| O(1) | 常数阶 | 1 | 1 | 1 |
| O(log n) | 对数阶 | 3 | 10 | 20 |
| O(n) | 线性阶 | 10 | 1000 | 1000000 |
| O(n log n) | 线性对数阶 | 30 | 10000 | 20000000 |
| O(n²) | 平方阶 | 100 | 1000000 | 10¹² |

注意O(log n)这一行：n从10增长到一百万，增长了十万倍，步数只从3增长到20。这意味着如果能把一个O(n)的操作改造成O(log n)，在大数据量下就是几万倍的提速。后面会看到，树这种逻辑关系之所以重要，很大程度上就是因为它能把查找从O(n)压到O(log n)。

反过来看O(n²)那一行，n为一百万时步数是一万亿，普通电脑要跑很久。一个O(n²)的算法在小规模测试时可能感觉不到问题，数据量上去后就会无法忍受。这也是为什么我们不能只靠"跑一下看看"来判断算法好坏。

### 1.5.5 空间复杂度

除了时间，算法还会占用内存空间，同样可以用大O来衡量，叫空间复杂度。上一节链式存储每个元素多占一个格子存地址，n个元素就多占n个格子，额外空间是O(n)；顺序存储不存关系，额外空间是O(1)。时间和空间之间也常常要做取舍，后面会陆续遇到。

> **到这里你知道了**：衡量算法不用秒，用步数随n增长的量级；大O是本书的尺子，O(1)、O(log n)、O(n)、O(n log n)、O(n²)之间的差距在n大时是天壤之别。
> **下一步**：把四层框架和全书的路线图放在一起。

## 1.6 四层框架与本书的组织

到这里，第1章开头的问题——"如何设计数据的组织方式，让管理和操作数据的效率更高"——里的每一个词都有了着落：数据之间的关系用逻辑结构描述，操作用抽象数据类型定义，组织方式落实为存储结构，效率用复杂度来衡量。有了大O这把尺子，后面每一章比较不同存储结构、不同算法的时候，就有了共同的语言。

于是本书的骨架也立起来了：

**逻辑结构 → 抽象数据类型 → 存储结构 → 算法**

逻辑结构描述数据之间的关系，抽象数据类型在逻辑结构上加上操作集，存储结构把抽象数据类型映射到内存，算法则是在存储结构上实现操作并衡量效率。

后面各章按逻辑关系的复杂程度依次展开，每一章回答的都是同一个问题：某种逻辑关系上的某个抽象数据类型，用什么存储结构实现，效率如何。

| 章 | 逻辑关系 | 抽象数据类型 | 存储结构 | 效率上的主要问题 |
|---|---|---|---|---|
| 1 绪论 | 四种 | 概念 | 顺序/链式 | 建立衡量语言 |
| 2 线性表 | 线性 | 线性表 | 数组、链表 | 按位置存取 vs 插入删除的取舍 |
| 3 栈和队列 | 线性 | 栈、队列 | 数组、链表 | 限制操作集后两种存储都能做到O(1) |
| 4 树 | 树形 | 树、二叉树 | 孩子指针、数组 | 层级关系的表示与遍历 |
| 5 集合与查找 | 集合 | 集合、字典 | 有序数组→二叉搜索树→平衡树→散列表 | 查找从O(n)到O(log n)到O(1) |
| 6 优先队列 | 集合 | 优先队列 | 堆 | 反复取最值的代价 |
| 7 图 | 图 | 图 | 邻接矩阵、邻接表 | 遍历、最短路径、最小生成树 |
| 8 排序 | 线性 | — | 数组为主 | 用前面的结构（堆、树）改进算法 |

第2、3章讲线性关系。线性表是最基本的抽象数据类型，两种存储方式的取舍在这里第一次完整呈现；栈和队列则展示了限制操作集之后，实现可以变得多简单。

第4章讲树形关系。除了表示层级数据本身，树还有另一个更重要的用途：它是后面把查找加速的工具。

第5、6章回到集合关系。集合和字典这两个抽象数据类型看起来简单，但"判断元素在不在"这一个操作，实现方式从有序数组的二分查找，到二叉搜索树，到平衡树，再到散列表，效率从O(n)逐步压到O(log n)和O(1)。这是全书最能体现"操作集决定存储选择"的部分。优先队列则是集合关系上另一个操作集不同的抽象数据类型，用第4章的树以堆的形式实现。

第7章讲图关系。图是最一般的关系，它的两种存储方式对应顺序和链式两种手段，遍历、最短路径、最小生成树等问题都在此展开。

第8章讲排序。排序不是新的逻辑关系，而是把前面学过的结构用起来：堆排序用堆，归并排序体现分治，各算法之间的效率比较是对第1章复杂度分析的一次总练习。

全书读完，回头看第1章提出的问题——"如何设计数据的组织方式，让管理和操作数据的效率更高"，答案就是这张表：先弄清数据之间的逻辑关系，再根据应用需要的操作定义抽象数据类型，然后在顺序和链式两种基本手段中选择或组合出存储结构，最后用复杂度分析验证这个选择是否达到了目的。
