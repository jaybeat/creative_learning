# 交接文档：《数据结构》书稿在线阅读站

> 读者：Claude Code（或任何接手实现的工程师）。
> 本文档是唯一的需求来源。先通读全文，再从「10. 里程碑」的 M1 开始动手。
> 标注【已实测】的结论是在真实书稿和真实字体文件上验证过的，不要凭直觉推翻；如果要改，先复现实测再改。

---

## 1. 背景与目标

作者正在写一本面向初学者的 C 语言版《数据结构》教材，书稿是 Markdown，一章一个文件，仍在持续改稿。目前手头有第 2 章（线性表，约 1600 行 / 4.1 万字），其余章节会陆续加入。

要做的东西：一个**静态的在线阅读站**，让学习者在浏览器（含手机）里按章节顺序阅读。体验参考 mdview / 文档站：左侧全书目录，中间正文，右侧本页小节目录。

成功标准只有三条：

1. 作者把 md 文件放进仓库、`git push`，网站自动更新，除此之外不需要做任何事，**也不需要为了网站改变 Markdown 的写法**。
2. 书里的字符画图示在 Windows / macOS / Android / iOS 上都严格对齐。
3. 手机上读起来舒服：不出现页面级横向滚动，能记住读到哪。

## 2. 不做的事（本期范围外）

- 不做后端、账号、评论、付费、跨设备同步。
- 不做在线编辑、不做 PDF 导出。
- 不做在线编译运行 C 代码、不做交互动画（第三期的事，但架构不能堵死，见 §9）。
- 不自研站点框架，不重写 VitePress 默认主题，只做扩展和样式覆盖。

## 3. 书稿特征【已实测，基于第 2 章】

实现时所有规则都要以这些事实为准。

| 特征 | 实测情况 | 对实现的含义 |
|---|---|---|
| 标题层级 | `#` 一个（`# 第2章 线性表`），`##` 12 个（`## 2.1 问题：单行编辑器` … `## 2.12 …`），`###` 若干（`### 2.3.1 结构定义`），编号规整 | 目录、切页、锚点全部自动生成 |
| 章引言 | `#` 和第一个 `##` 之间**没有**正文 | 章首页需要自动生成；但其他章可能有引言，要兼容 |
| 代码块 | 共 74 个：55 个 ` ```c `，19 个无语言标记 | 见下两行 |
| C 代码 | 24 个含中文注释；6 个含 `int main`；3 个 ≥ 40 行，最长 80 行；最宽一行 195 列 | 等宽字体必须覆盖中文；长代码折叠；**不折行**，块内横向滚动 |
| 无标记块 | 17 个是字符画图示（含 `┌─┬┐│└┴┘┼╱▲►▼◄●`），其中 8 个混有中文标注（如 `p 第0步`）；其余 2 个是终端会话示例 | 字符画对齐是本项目头号技术风险，见 §6 |
| 表格 | 62 行表格内容；有一张 17 列的宽表（顺序表下标示意） | 表格容器横向滚动，宽表单元格不换行 |
| 引用块 | 6 个，全部是「里程碑提示」，形如 `> **到这里你有了**：…` 换行 `> **下一步**：…`。出现过的标签：到这里你有了(6)、下一步(6)、用到的操作、还没解决的、接口、验证了、学到的 | 渲染成卡片，见 §5.4 |
| 交叉引用 | 「2.6节」这类 29 处；「第1章」「第3章」共 9 处 | 自动链接，见 §5.3 |
| 粗体独占一行 | 如 `**初始化**`、`**结点从哪来**`，作者把它当四级小标题用 | 样式上略加强调即可，不进目录 |
| 图片 / LaTeX / mermaid / HTML | 全部为 0（用 Unicode 下标 a₀、aₙ₋₁ 代替公式；唯一的 `<` 是行内代码里的 `#include <stdlib.h>`） | 不需要 KaTeX、mermaid；但要保证 md 里的 `<stdlib.h>` 这类文本不被当成 HTML/Vue 标签 |

**两个已确认的陷阱**，必须有单元测试覆盖：

- **陷阱 A：代码块里的 `#define`。** 书稿第 45 行等处有 `#define MAXSIZE 100`，位于 ` ```c ` 围栏内、行首。按 `^#` 切分标题会把它误判成一级标题。切页必须感知代码围栏（建议用 markdown-it 的 token 流找 heading，而不是对源码跑正则）。
- **陷阱 B：版本号与节号撞车。** 正文里反复出现「编辑器1.0 / 1.1 / 2.0 / 2.1 / 2.2」，而本章恰好也有 2.1 节、2.2 节。交叉引用**只匹配带「节」字后缀的写法**（`2.6节`、`2.4.3节`），裸数字一律不动。

## 4. 技术选型

- **VitePress**（最新稳定版）+ 默认主题扩展。理由：三栏布局、侧栏、页内大纲、上下页、深色模式、响应式、Shiki 高亮、代码复制按钮、本地搜索全部内置；md 里可直接用 Vue 组件，为第三期留了口子。
- **Node ≥ 20**，包管理器用 npm，语言用 TypeScript（脚本可用 `.mjs`/`.ts`，自行决定）。
- 测试：**vitest**（单元）+ **Playwright**（字宽与布局的浏览器验收）。
- 字体子集化：`subset-font`（npm，基于 harfbuzz wasm）或等价方案；字宽校验用 `fontkit` 或 `opentype.js`。
- 部署：GitHub Actions → GitHub Pages。`base` 路径通过环境变量注入，方便以后换到 Cloudflare Pages 或国内对象存储。

VitePress 的具体配置项名称（尤其是本地搜索的 miniSearch 选项、`transformPageData`、`markdown.config` 钩子）**以官方文档当前版本为准**，动手前先查 vitepress.dev，不要凭记忆写。

## 5. 内容管线

### 5.1 仓库结构

```
/
├─ HANDOFF.md                  本文档
├─ book/                       ← 作者唯一需要碰的目录
│  ├─ book.yml                 书的元数据与章节清单
│  ├─ chapters/
│  │  └─ ch02.md               书稿原文，原样放入，不做任何手工修改
│  └─ code/                    （预留）各版本完整源码，本期不用
├─ scripts/
│  ├─ build-content.ts         切页 + 生成侧栏清单 + 交叉引用索引
│  ├─ build-font.ts            扫描字符 → 子集化 → 字宽校验
│  └─ lib/                     splitter / xref / charset 等纯函数，供测试
├─ tools/fonts/
│  ├─ SarasaFixedSC-Regular.ttf   字体源文件（约 14 MB，直接提交进仓库）
│  └─ LICENSE-Sarasa.txt          SIL OFL 1.1
├─ site/                       VitePress 根目录
│  ├─ .vitepress/
│  │  ├─ config.ts
│  │  ├─ theme/                默认主题扩展：组件 + 样式
│  │  └─ generated/            sidebar.json、xref.json（构建产物，gitignore）
│  ├─ public/fonts/            子集 woff2（构建产物，gitignore）
│  ├─ index.md                 首页（书名、简介、继续阅读、章节列表）
│  └─ ch02/ …                  切出来的页面（构建产物，gitignore）
├─ tests/
└─ .github/workflows/deploy.yml
```

`book.yml` 示例：

```yaml
title: 数据结构
subtitle: （作者稍后补）
author: （作者稍后补）
chapters:
  - file: ch02.md        # 章号、章标题从文件里的「# 第N章 标题」解析，不在这里重复写
    draft: false
  # - file: ch03.md
  #   draft: true        # draft 的章：不生成页面，侧栏显示为灰色「即将发布」
```

作者上传的原始文件名可能很乱（如 `第2章_线性表拆分2_0.md`）。约定放进仓库时改名为 `chNN.md`；章号以文件内的 `# 第N章` 为准，与文件名不一致时构建报错。

### 5.2 切页规则（`build-content`）

输入一章一个 md，输出一节一页。

- 用 markdown-it 解析出 token 流，定位 `h1` / `h2` 的源码行号（`token.map`），再按行号切原文。**不要对源码跑 `^#` 正则**（陷阱 A）。
- `# 第N章 标题` → 章。`## N.M 标题` → 一页。`###` 保留在页内，作为右侧大纲。
- URL 用纯数字 slug，**不要出现中文**（中文 URL 在微信等场景分享时会变成一长串百分号编码）：
  - 章首页：`/ch02/`
  - 节页面：`/ch02/2-8`（对应 `## 2.8 …`）
  - 小节锚点：`#2-8-1`（对应 `### 2.8.1 …`）。需自定义标题 slug 规则：标题以编号开头就用编号，否则回退到 VitePress 默认规则。
- 每个生成的页面写入 frontmatter：`title`（完整节标题）、`chapter`、`section`、`prev` / `next`（跨章也要接上；全书第一页无 prev，最后一页无 next）。
- 页面正文的 `## 2.8 标题` 在生成时提升为该页的 `# 2.8 标题`，`###` 相应提升为 `##`，这样 VitePress 的页内大纲无需特殊配置。
- 章首页：`#` 与第一个 `##` 之间若有正文，作为章首页内容；若没有（第 2 章就是这样），自动生成——章标题 + 本章各节的链接列表（带已读标记）+「开始阅读 / 继续阅读」按钮。
- 同时产出 `generated/sidebar.json`（章 → 节两级，章可折叠，当前章默认展开）和 `generated/xref.json`（`"2.8" → "/ch02/2-8"`，`"2.8.1" → "/ch02/2-8#2-8-1"`，`"ch2" → "/ch02/"`）。
- `npm run dev` 时监听 `book/`，文件变化后自动重新切页，VitePress 热更新。

### 5.3 交叉引用（markdown-it 插件）

- 只处理 **inline 的 `text` 类型 token**。代码块、行内代码、标题、已有链接内的文字一律不碰。
- 规则 1：`(?<![\d.])(\d+)\.(\d+)(?:\.(\d+))?节` → 链接到对应节 / 小节，链接文字保持原样（如「2.6节」）。
- 规则 2：`第(\d+)章` → 链接到章首页；指向当前章时不加链接。
- 目标不存在（如现在的「第1章」「第3章」）：保持纯文本，构建时打印**警告**（含源文件名和行号），不要让构建失败。构建结束时汇总一份「未解析的引用」清单，这对作者查稿有用。
- 陷阱 B 的测试用例：`编辑器2.2`、`编辑器1.1` 不得被链接；`2.2节`、`2.4.3节` 必须被链接；`` `2.6节` ``（行内代码里）不得被链接。

### 5.4 里程碑卡片（markdown-it 插件）

- 识别条件：blockquote 的第一段以 `**到这里你有了**` 开头。
- 这类引用块里各行之间没有空行，markdown-it 会解析成**一个段落、多行用 softbreak 分隔**。按 softbreak 拆行，每行形如 `**标签**：内容`，渲染成卡片里的一行（标签 + 内容）。
- 标签 → 图标/颜色的映射表：到这里你有了（✓，主色）、下一步（→）、还没解决的（!，警示色）、学到的 / 验证了 / 用到的操作 / 接口（中性）。**未知标签走默认样式**，不要报错——作者以后会发明新标签。
- 不满足识别条件的普通引用块按默认样式渲染。
- 作者的 Markdown 写法不变；在不支持此插件的普通预览器里它仍是一个正常的引用块。

### 5.5 代码块

- ` ```c `：Shiki 高亮（构建期完成），亮/暗各配一个主题。保留 VitePress 自带的复制按钮。
- 无语言标记的块按内容自动分类，挂不同的 class：
  - 含 Box Drawing 字符（U+2500–U+257F）→ `diagram`：不高亮、不显示复制按钮和语言角标、背景更淡、行高收紧到竖线 `│` 上下能接上（约 1.25–1.3，以实际渲染为准）。
  - 否则 → `session`：终端会话样式，保留复制按钮。
- 所有代码块：`white-space: pre`，**绝不自动折行**，超宽时块内 `overflow-x: auto`。
- 超过 40 行的 C 代码块默认折叠，露出前 20 行 + 渐隐 +「展开全部（共 N 行）」按钮；阈值写成配置项。折叠用纯 CSS + 少量 JS，无 JS 时默认展开。

### 5.6 表格

- 每个表格包一层可横向滚动的容器（先确认 VitePress 默认主题是否已处理，没有再加）。
- 列数 > 8 的表格：单元格 `white-space: nowrap`、内边距收紧，让 17 列那张表保持网格形态而不是被挤成竖排。

## 6. 字符画对齐方案【已实测】—— 头号风险，单独成节

### 6.1 问题

书稿里的图示按「中文 = 2 列，其余一切字符（含制表符、箭头、圆点）= 1 列」画的。例如：

```
head
  │
  ▼
┌───┬───┐     ┌───┬───┐     ┌───┬───┐
│ c │ ●─┼────►│ a │ ●─┼────►│ t │ ╱ │
└───┴───┘     └───┴───┘     └───┴───┘
  p 第0步       p 第1步       p 第2步
```

`► ▼ ▲ ◄ ●` 和制表符在 Unicode 里属于「东亚模糊宽度」，多数西文等宽字体缺这些字形，浏览器会回退到系统中文字体并渲染成全宽，图就歪了，且各平台歪法不同。靠系统字体栈无解，必须下发 webfont。

### 6.2 字体选择：**Sarasa Fixed SC（更纱黑体 Fixed SC）Regular**

实测结果（v1.0.41，`SarasaFixedSC-TTF-Unhinted-1.0.41.7z`，unitsPerEm = 1000）：

| 字体 | ASCII | `─│┼╱` | `▲►▼◄●` | `→←↑↓…—` | 中文 / `，：` | 结论 |
|---|---|---|---|---|---|---|
| Sarasa **Mono** SC | 500 | 500 | **1000** | — | 1000 | ✗ 箭头和圆点是全宽，图会歪 |
| Sarasa **Term** SC | 500 | 500 | 500 | 500 | 1000 | ✓ 宽度正确，但带连字 |
| Sarasa **Fixed** SC | 500 | 500 | 500 | 500 | 1000 | ✓✓ 宽度正确且**无连字** |

- **不要用 Sarasa Mono SC**，这是最容易犯的错（名字看起来最像正确答案）。
- 选 Fixed 而不是 Term：这是一本讲指针的书，`p->next` 满篇都是，连字会把 `->` 画成一个箭头字形，对初学者是误导。CSS 里仍然加 `font-variant-ligatures: none` 作为保险。
- 用第 2 章全部代码块 + 行内代码的字符集（211 个字符）校验：Sarasa Fixed SC 全覆盖、零宽度违规；子集化为 woff2 后约 **20 KB**。用该字体渲染上面的图示，目视确认完全对齐。
- 许可证 SIL OFL 1.1，允许子集化后随站点分发；把许可证文件放在字体旁边。CSS 里的 `font-family` 起一个中性名字（如 `BookMono`）。

### 6.3 构建步骤（`build-font`）

1. 扫描 `book/chapters/` 下**所有**章节的代码块与行内代码，收集字符集；并上 ASCII 可打印字符 U+0020–U+007E。
2. 用该字符集对 `tools/fonts/SarasaFixedSC-Regular.ttf` 做子集，输出 `site/public/fonts/bookmono.[hash].woff2`，关闭 layout features（不需要连字等）。
3. **字宽校验（构建期硬性检查，失败则构建失败）**：字符集中每个字符必须在字体里有字形；CJK 统一表意文字、CJK 标点（U+3000–303F）、全角形式（U+FF00–FFEF）的 advance 必须是 1000，其余必须是 500。违规时打印出具体字符和它所在的源文件行。——这条检查的意义在于：作者以后若在图里用了某个新符号而字体不支持或宽度不对，构建当场报出来，而不是上线后才发现图歪了。
4. 在 `<head>` 里 `preload` 这个字体文件，`@font-face` 用 `font-display: swap`。

### 6.4 CSS 要点

```css
/* 代码块、行内代码、图示统一用它 */
font-family: BookMono, "Sarasa Fixed SC", "Sarasa Term SC", Consolas, Menlo, monospace;
font-variant-ligatures: none;
font-synthesis: none;      /* 只下发了 Regular，禁止浏览器合成粗体/斜体 */
```

- 代码块内**强制** `font-weight: 400; font-style: normal`（包括 Shiki 主题给注释加的斜体、给关键字加的粗体都要压掉）。合成粗体在部分浏览器里会改变字宽，破坏对齐。
- `tab-size: 4`；`letter-spacing: 0`；不要给代码块设置 `text-align: justify` 或任何会影响字距的属性。
- 手机上图示字号可以比正文代码略小（如 13px），宁可块内横向滚动也不折行。

## 7. 阅读界面

在 VitePress 默认主题上扩展，不重写。

- **布局**：桌面三栏（左：全书目录；中：正文；右：本页小节大纲，随滚动高亮）。平板隐藏右栏。手机左栏变抽屉、大纲收进顶部下拉——这些默认主题已有，确认行为正确即可。
- **正文排版**：内容区最大宽度 ~720px；中文正文行高 1.8 左右，段间距适中；正文字体用系统中文字体栈，不下发正文 webfont。
- **上一节 / 下一节**：页底导航，跨章连续。支持键盘 ← → 翻节（焦点在输入框、搜索框时不触发）。
- **阅读进度**（纯前端，`localStorage`）：
  - 记录最近阅读的页面与滚动位置；首页和章首页显示「继续阅读：2.9 链表的六个操作与头结点」。
  - 页底导航进入视口（IntersectionObserver）即视为该节已读；侧栏和章首页对已读节显示 ✓。
  - 首页提供「清除阅读记录」。
  - VitePress 是 SSR/SSG：所有 `localStorage` 访问放在 `onMounted` 之后，首屏输出不依赖它，避免 hydration mismatch；读写全部 try/catch（隐私模式下可能抛异常）。
- **字号调节**：小 / 中 / 大三档，作用于正文与代码，记入 `localStorage`。
- **深色模式**：用默认主题自带的；检查里程碑卡片、图示块、折叠渐隐在两种模式下都正常。
- **标题锚点**：hover 标题出现 `#` 链接，便于老师在群里分享具体小节。
- **搜索**：VitePress 本地搜索。中文需要自定义分词（可用 `Intl.Segmenter('zh', {granularity:'word'})`，不可用时回退到按字/二元切分）。验收：搜「头结点」「扩容」「malloc」「值传递」都能命中正确的节，结果显示节标题。
- **首页**：书名、一句话简介、继续阅读、章节列表（draft 章灰显「即将发布」）。朴素即可，不要营销式大图首页。
- **页脚**：每页一个「本页有错？反馈」链接（指向仓库 issue，带上页面路径），仓库地址从 `book.yml` 读，未配置则不显示。

视觉风格：克制、像一本书而不是产品官网。不加动效，不加装饰性插画。

## 8. 部署

- `.github/workflows/deploy.yml`：push 到 `main` → `npm ci` → `npm test` → `npm run build` → 发布到 GitHub Pages。缓存 npm。
- `npm run build` = `build-font` → `build-content` → `vitepress build`。任何一步失败整体失败。
- `base` 从环境变量读取（GitHub Pages 项目站需要 `/<repo>/`，自定义域名或其他平台是 `/`）。
- README 里用中文写清楚作者的日常流程：①把新章节存为 `book/chapters/chNN.md`；②在 `book.yml` 加一行；③push。以及本地预览命令。

## 9. 为后续阶段预留（本期不实现，但不要堵死）

- **代码版本对比**：书中编辑器从 1.0 演进到 2.2，2.10 节的卖点是「main 一行不改」。将来会在 `book/code/` 放各版本完整源码，并提供一个 `<CodeDiff from="editor-1.0/main.c" to="editor-2.0/main.c" />` 组件。所以：保留 md 中使用 Vue 组件的能力；同时注意 §3 提到的 `<stdlib.h>` 不能因此被误解析（它目前只出现在行内代码里，是安全的，但请加一个回归测试）。
- **分步动画**：2.9.2 节「第一步…第四步」的连续字符画天然是动画分镜，将来可能做成步进器。切页脚本里保留每个 `diagram` 块的稳定 id（如 `ch02-2-9-d03`），以后好挂载。
- **完整源码下载 / 在线编译器跳转**、**每节自测题**：暂不考虑。

## 10. 里程碑与验收

每个里程碑结束时停下来，给作者一个可以本地预览的状态和一段简短说明，再继续下一个。

### M1　能读（核心）

范围：§5.1 仓库骨架、§5.2 切页、§5.5 代码块（折叠可放到 M2）、§5.6 表格、§6 字体全部、§7 中的布局/排版/上下节、§8 部署。

验收：
- [ ] `npm run build` 通过；第 2 章生成 1 个章首页 + 12 个节页面，URL 形如 `/ch02/2-8`。
- [ ] 单元测试：陷阱 A（围栏内 `#define` 不被当标题）；prev/next 链完整；`###` 锚点为 `#2-8-1` 形式。
- [ ] 字宽校验脚本通过；故意往测试夹具的图示里加一个字体不支持的字符，构建应失败并指出位置。
- [ ] Playwright（Chromium + WebKit，桌面与 375px 手机视口）：在 `diagram` 块的字体环境下，`a ─ │ ┼ ╱ ► ▼ ▲ ◄ ●` 各自重复 20 次的渲染宽度两两相等；`中` 重复 20 次的宽度恰为其 2 倍（允许 ≤ 0.5px 误差）。
- [ ] Playwright：375px 视口下，每个页面 `document.documentElement.scrollWidth <= clientWidth`（无页面级横向滚动）；宽表格和长代码在各自容器内滚动。
- [ ] 目视：2.8.1、2.9.2、2.12.4 的图示对齐，竖线上下相接；`p->next` 没有变成连字箭头；代码块内没有粗体/斜体。

### M2　好读

范围：§5.3 交叉引用、§5.4 里程碑卡片、长代码折叠、搜索、阅读进度、字号调节、键盘翻页、锚点链接、首页与章首页、反馈链接。

验收：
- [ ] 单元测试：陷阱 B 全部用例；里程碑卡片对 7 种已知标签和 1 个未知标签的渲染；普通引用块不受影响。
- [ ] 构建日志输出未解析引用清单（当前应包含「第1章」「第3章」的若干处），构建不失败。
- [ ] 搜索四个关键词命中正确。
- [ ] 刷新后「继续阅读」指向正确；无 hydration 警告；隐私模式下不报错。
- [ ] Lighthouse（移动端）性能与可访问性均 ≥ 90。

### M3　（以后）

§9 的内容，等作者另行确认后再做。

## 11. 需要向作者确认的事项

不阻塞 M1，用占位值先做，在 M1 交付时一并询问：

1. 书名、副标题、作者署名、一句话简介。
2. GitHub 仓库是公开还是私有；是否有自定义域名；主要读者是否在中国大陆（决定是否需要换国内托管）。
3. 未完成的章节是「完全隐藏」还是「目录里灰显为即将发布」（当前默认后者）。
4. 40 行折叠阈值是否合适，还是希望代码全部默认展开。

## 附：实测环境备忘

- 字体来源：`https://github.com/be5invis/Sarasa-Gothic/releases`，实测版本 v1.0.41。各变体是独立的 7z 包（`SarasaFixedSC-TTF-Unhinted-<ver>.7z`），解压后取 `SarasaFixedSC-Regular.ttf`（约 14 MB）提交到 `tools/fonts/`。换版本后必须重跑字宽校验。
- 实测方法：fontTools 读取 `hmtx` advance；子集化用 `fontTools.subset`（woff2，无 layout features）。Node 侧用等价工具复现即可，数值应一致（500 / 1000）。
